import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Examen_Barrera_Kaysy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
